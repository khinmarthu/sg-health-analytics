PORT=5000
RESOURCE_ID=d_0d1da54a73733d33e40f662f757af537
DATA_GOV_BASE_URL=https://data.gov.sg/api/action/datastore_search
CACHE_TTL_SECONDS=300
# Optional — only raises data.gov.sg's rate limit, app works without it.
# Get one at https://guide.data.gov.sg. In CI this is a GitHub Actions
# repository secret instead, not stored in a file.
DATA_GOV_API_KEY=
